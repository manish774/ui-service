export interface signupProps {
  firstName: string;
  lastName: string;
  emailId: string;
  string: string;
  gender: string;
  skills: any;
}

export interface loginProps {
  email: string;
  password: string;
}
